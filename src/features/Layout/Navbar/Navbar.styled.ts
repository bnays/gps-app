import styled from "styled-components";

export const NavbarWrapper = styled.div`
    text-align: right;
    .logout {
        font-weight: bold;
        position: relative;
        top: 15px;
    }
`;
