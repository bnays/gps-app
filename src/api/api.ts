export const baseUrl = process.env.REACT_APP_API_BASE_URL;

// Sign up
export const signupApi = `${baseUrl}/api/signup`;

// Login
export const loginApi = `${baseUrl}/api/login`;

// Gps Data
export const fetchGpsDataApi = `${baseUrl}/api`;
